<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.fandemic.impl.MemberMapper">
<!-- 멤버 매퍼 아니고 로그인 메퍼!!!! -->



<!-- 개인 로그인  -->
	<select id="memLogin" parameterType="Member" resultType="Member">
		SELECT * FROM MEMBER 
		WHERE MEM_ID= #{mem_id} 
		AND MEM_PW= #{mem_pw}
	</select> 
	
<!-- 소속사 로그인  -->
	<select id="comLogin" parameterType="Company" resultType="Company">
	
		SELECT * FROM COMPANY 
		WHERE COM_ID= #{com_id} 
		AND COM_PW= #{com_pw}
	</select> 
	
<!-- 아이디 중복확인 -->
	<select id="memIdCheck" parameterType="Member" resultType="int">
		SELECT COUNT(*) 
		FROM MEMBER 
		WHERE MEM_ID=#{mem_id}
	</select>

<!-- 아이디 중복확인 -->
	<select id="comIdCheck" parameterType="Company" resultType="int">
		SELECT COUNT(*)   
		FROM COMPANY  
		WHERE COM_ID=#{com_id}
	</select>
	
<!-- 개인 회원가입 -->
	<insert id="memInsert" parameterType="Member">
		INSERT INTO MEMBER (MEM_ID, MEM_NAME, MEM_PW, MEM_BIRTH, MEM_PHONE, MEM_EMAIL, MEM_ADDRESS, 
							MEM_ADDRESS2,MEM_TYPE, MEM_GENDER, MEM_ZIPADDRESS)
		VALUES(
							#{mem_id}, #{mem_name}, #{mem_pw}, #{mem_birth}, #{mem_phone},
							#{mem_email}, #{mem_address}, #{mem_address2}, 1, #{mem_gender}, #{mem_zipAddress} )

		
	</insert>

<!-- 기업 회원가입 -->
	<insert id="comInsert" parameterType="Company">
		INSERT INTO COMPANY (COM_ID, COM_PW, COM_NAME, COM_PHONE, COM_ADDRESS, 
							 COM_ADDRESS2, COM_EMAIL, COM_ZIPADDRESS) 
		VALUES( 
							#{com_id}, #{com_pw}, #{com_name}, #{com_phone}, #{com_address}, 
							#{com_address2}, #{com_email}, #{com_zipAddress} )

	</insert>

</mapper>
